import React from 'react';

const Home = {

  render: () => {
    return (
      <section className="panel well">
        hello world!
      </section>
    );
  }
}

module.exports = React.createClass(Home);
